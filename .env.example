# This file is a template for environment variables.
# 1. Copy this file to a new file named ".env" (which is ignored by git).
# 2. Fill in the secret values in your local .env file.

# === PostgreSQL Database ===
# These values must match the 'environment' section in docker-compose.yml
DATABASE_URL="postgresql://user:password@localhost:5432/yultimate_db"

# === FastAPI Backend ===
# Generate a strong, random key (e.g., using `openssl rand -hex 32`)
JWT_SECRET_KEY="YOUR_SUPER_SECRET_RANDOM_KEY_GOES_HERE"
JWT_ALGORITHM="HS256"
